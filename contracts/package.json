{
  "name": "finpass-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for FinPass DID Registry",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:mumbai": "hardhat run deploy.js --network mumbai",
    "deploy:local": "hardhat run deploy.js --network hardhat",
    "test": "hardhat test",
    "verify": "hardhat verify --network mumbai"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.2",
    "hardhat": "^2.17.2",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  }
}
